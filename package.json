{
  "name": "frostflare",
  "private": true,
  "version": "1.0.0",
  "description": "Local-first 3D Discord activity visualization",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "npm ci --prefix backend && npm ci --prefix frontend",
    "demo": "npm run demo --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "check:docs": "node scripts/check-doc-links.mjs",
    "check": "npm run check:docs && npm run type-check --prefix backend && npm run build --prefix backend && npm run type-check --prefix frontend && npm run build --prefix frontend"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/FrozenScorch/frostflare.git"
  },
  "license": "MIT"
}
